To Update OS:
	sudo apt-get update
	sudo apt-get upgrade


GitHub commands:
	Modify git options:
		nano .git/config
	
	Add a file
		git add <filename>

	Commit a change:
		git commit -m "message"
			or
		git commit -a

	git push

	1) Add or modify a file
	2) git add "filename"
	3) git commit -m "message"
	4) git push